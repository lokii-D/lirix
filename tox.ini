[tox]
envlist = py39, py310, py311, py312, py313, py314
isolated_build = true
skip_missing_interpreters = true

[testenv]
extras = dev
setenv =
    PYTHONIOENCODING = utf-8
    PYTHONUTF8 = 1
commands =
    black --check .
    ruff check .
    mypy .
    pytest {posargs}
