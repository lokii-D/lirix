[tox]
envlist = py39, py310, py311, py312, py313, py314
isolated_build = true
skip_missing_interpreters = true

[testenv]
extras = dev
commands =
    black --check .
    ruff check .
    mypy .
    pytest {posargs}
