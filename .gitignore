# Exclusion SSOT: docs/repo_exclusions.md — mirror paths here; see § Three-file maintenance rule (also update .harnessignore, pytest norecursedirs, audit map Scope).

# Virtual environments
.venv/
.venv*/
.venv*
.venv_ci*/
.venv_cursor*/
.venv312/
venv/
ENV/
env/

# Environment files / secrets
.env/
.env
.env.*
!.env.example
!.env.release.example
*.env
.env.release
.secret
.secrets
*.key
*.pem
*.crt
*.p12
*.pfx

# Python caches and build artifacts
__pycache__/
*.pyc
*.pyo
*.pyd
*.py[cod]
*.so
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/
.coverage
.coverage.*
htmlcov/
site/
.build/
build/
dist/
*.egg-info/
*.dist-info/

# Test / benchmark / local output artifacts
out/
output/
artifacts/
logs/
*.log
benchmark-artifacts*/
benchmarks-artifacts*.md
benchmarks-todos-output*.md
change_log/
.pendingchanges/
pendingchanges/

# Cursor / editor / workspace state
.cursor/
.cursor-cache/
.cursorignore
.cursorrules
worktrees.json
*.code-workspace

# OS junk
.DS_Store
Thumbs.db

# Package / archive outputs
*.tar
*.tar.gz
*.tgz
*.zip
mantle-harness.tar.gz
/dist/mantle_TT_submission_bundle.tar.gz
/dist/*.tar.gz
/dist/*.zip

# Misc local state
*.swp
*.swo
*~

# Root Mantle harness duplicates (SSOT: mantle_TT/) — prevents mainline lint/pytest pollution
/README_submission.md
/app.py
/tests/mantle/
/Dockerfile.submission
/requirements_submission.txt
/docker-compose.yml

# JavaScript tooling (if introduced locally)
node_modules/

# IDE / editor (local only)
.cursor/

# Local research/checklist artifacts (keep private)
checklists/
prompts/
tdsc/
mantle_TT/

# Audit artifacts: only version release signoff evidence
audit_artifacts/**
!audit_artifacts/release_signoff/
!audit_artifacts/release_signoff/**

# Dev silos (local only)
.lirix_launch_silo/
.lirix_audit_venv/
.lirix_final_preflight_venv/
.mantle_tt_venv/
_lirix_launch_sandbox/
