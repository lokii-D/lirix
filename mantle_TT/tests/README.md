# Tests

This folder contains Mantle bundle validation tests for submission integrity and baseline configuration checks.

Run from repository root:

```bash
./.venv/bin/python -m pytest -q mantle_TT/tests
```

For full project confidence, run:

```bash
./.venv/bin/python -m pytest -q
```
