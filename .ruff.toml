target-version = "py39"
line-length = 100
src = ["lirix"]

[lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
# Keep conservative annotation transforms across supported versions.
ignore = ["UP006", "UP007", "UP035", "UP045"]

[lint.per-file-ignores]
"lirix/tests/**" = ["S101"]
"tests/**" = ["E501"]
